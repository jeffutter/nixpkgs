#!/usr/bin/env bash
set -euo pipefail

FLAKE_DIR="${HOME}/.config/home-manager"

# Update flake inputs
echo "Updating flake inputs..."
cd "${FLAKE_DIR}"
git pull
nix flake update
bin/update-thaw
bin/update-claude-code
